# バグ修正テスト手順

## 問題の概要
リビルド後、Ensemble Modeで「出力を待機中...」が永続する問題

## 修正内容
ハンドシェイクプロトコルのタイミング問題を修正
- バックエンドのリスナー登録を非同期タスクの**外**に移動

## テスト手順

### 1. Tauriアプリを再起動
```bash
# 現在のdev serverを停止 (Ctrl+C)
# 再起動
npm run tauri dev
```

### 2. ブラウザを開く
http://localhost:5173/

### 3. Ensemble Mode実行
1. "Run AI Task" ボタンクリック
2. "Ensemble Mode" 選択
3. タスク入力: `ファイル一覧を表示して`
4. Instances: `2`
5. "Run" クリック

### 4. 確認ポイント

#### ✅ 成功の場合
- 2-3秒以内に出力が表示される
- リアルタイムでテキストが更新される
- ターミナルに以下のログが表示:
  ```
  ✅ [HANDSHAKE] Frontend ready signal received
  🚀 Starting monitoring for instance 1
  📤 Preparing to emit event 'competition-output'
  ```

#### ❌ 失敗の場合
- 「出力を待機中...」が5秒以上続く
- ターミナルに以下のログが表示:
  ```
  ⚠️ [HANDSHAKE] Frontend ready signal timeout
  ```

### 5. ブラウザコンソール確認
F12 → Consoleタブ

**期待されるログ**:
```
[Frontend] Registering listener for competition ...
[Frontend] Ready signal sent for competition ...
[Frontend] Received competition-output event: instance=1
```

## トラブルシューティング

### 問題: ターミナルに `[HANDSHAKE]` ログが表示されない
**原因**: Rustコードがコンパイルされていない
**対処**:
```bash
cd src-tauri
cargo clean
cd ..
npm run tauri dev
```

### 問題: ブラウザに出力が表示されない
**対処**:
1. ブラウザをリフレッシュ (Cmd+R / Ctrl+R)
2. ブラウザのキャッシュをクリア (Cmd+Shift+R / Ctrl+Shift+R)
3. dev serverを再起動

## バグが再現した場合

以下の情報を収集してください:
1. ターミナルのログ全体
2. ブラウザコンソールのログ
3. 実行した手順の詳細

ログファイル出力:
```bash
npm run tauri dev 2>&1 | tee /tmp/ait42-debug.log
```
