# Phase 10: Cursor風UI実装計画

## 目標

AIT42 EditorのUIを**Cursor**のような**モダンな3ペインレイアウト**にする

```
┌─────────────────────────────────────────────────────────┐
│ 🅰 AIT42              main.rs  lib.rs  test.rs     × □ ─│ ← タブバー
├─────────┬───────────────────────────────────────────────┤
│ 📁 FILES│  src/main.rs                          1:1 100%│
│ ─────── │                                               │
│ ▼ src   │  1  fn main() {                               │
│   main.rs  2    println!("Hello, World!");              │
│   lib.rs│  3  }                                         │
│ ▼ tests │  4                                            │
│   unit  │  5                                            │
│         │                                               │
│ ▼ docs  │                                               │
│   README│                                               │
├─────────┼───────────────────────────────────────────────┤
│ TERMINAL│ $ cargo build                                 │
│         │   Compiling ait42 v0.1.0                      │
│         │   Finished release [optimized] target(s)      │
└─────────┴───────────────────────────────────────────────┘
```

## 実装範囲

### ✅ Phase 10a: 基本レイアウト（UI表示のみ）
1. タブバー表示
2. サイドバー（ファイルツリー）表示
3. エディタエリア表示
4. ターミナルパネル表示
5. モダンなカラースキーム

### ⏳ Phase 10b: インタラクション（後日実装）
- タブ切り替え
- ファイルツリーナビゲーション
- ターミナルコマンド実行
- パネルリサイズ
- キーボードショートカット

## 使用するAIエージェント

### 1. System Architect Agent
**タスク**: Cursor風UI設計
```bash
タスク:
- 3ペインレイアウトアーキテクチャ設計
- Ratatui Widgetコンポーネント設計
- 状態管理設計（EditorState拡張）
- レンダリングパイプライン設計

成果物:
- アーキテクチャ図
- コンポーネント構成図
- データフロー図
```

### 2. UI/UX Designer Agent
**タスク**: Cursor風カラースキームとレイアウト設計
```bash
タスク:
- Cursorのカラーパレット分析
- ダークテーマ設計（背景色、前景色、アクセント色）
- アイコン・絵文字の選定
- スペーシング・パディング設計

成果物:
- カラーパレット定義
- テーマ定義ファイル (theme_cursor.rs)
```

### 3. Frontend Developer Agent
**タスク**: TUIウィジェット実装
```bash
タスク:
- TabBar Widget実装
- Sidebar Widget実装
- TerminalPanel Widget実装
- Ratatui rendering logic実装

成果物:
- widgets/tab_bar.rs
- widgets/sidebar.rs
- widgets/terminal_panel.rs
- renderer.rs更新
```

### 4. Feature Builder Agent
**タスク**: マルチパネルレイアウト機能統合
```bash
タスク:
- layout.rs拡張（タブバー、ターミナル対応）
- tui_app.rs統合（状態管理）
- イベントハンドリング

成果物:
- layout.rs更新
- tui_app.rs更新
- 統合テスト
```

### 5. Test Generator Agent
**タスク**: UIテストコード生成
```bash
タスク:
- レイアウト計算テスト
- ウィジェット描画テスト
- 状態遷移テスト

成果物:
- tests/ui/layout_tests.rs
- tests/ui/widget_tests.rs
```

### 6. Code Reviewer Agent
**タスク**: 実装コードレビュー
```bash
タスク:
- コード品質チェック
- Rustベストプラクティス検証
- パフォーマンスレビュー
- セキュリティチェック

成果物:
- レビューレポート
- 改善提案リスト
```

## 実装ステップ

### Step 1: アーキテクチャ設計
```bash
エージェント: SystemArchitect
時間: 30分

タスク:
1. 現在のTUIアーキテクチャ分析
2. Cursor風レイアウト設計
3. コンポーネント設計
4. データフロー設計

成果物:
- docs/architecture/cursor_ui_design.md
```

### Step 2: テーマ設計
```bash
エージェント: UIUXDesigner
時間: 20分

タスク:
1. Cursorのカラー分析
2. ダークテーマ定義
3. アイコン選定

成果物:
- crates/ait42-tui/src/themes/cursor.rs
```

### Step 3: レイアウト拡張
```bash
エージェント: FeatureBuilder
時間: 40分

タスク:
1. layout.rs拡張
   - show_tabs: bool追加
   - show_terminal: bool追加
   - terminal_height: u16追加
2. EditorLayout構造体更新
   - tabs: Option<Rect>追加
   - terminal: Option<Rect>追加
3. レイアウト計算ロジック更新

成果物:
- crates/ait42-tui/src/layout.rs (更新)
```

### Step 4: ウィジェット実装
```bash
エージェント: FrontendDeveloper
時間: 2時間

タスク:
1. TabBarウィジェット
   - タブリスト描画
   - アクティブタブハイライト
   - タブボタン（×□─）

2. Sidebarウィジェット
   - ファイルツリー描画
   - ディレクトリ階層表示
   - アイコン表示

3. TerminalPanelウィジェット
   - ターミナル出力表示
   - スクロール機能
   - プロンプト表示

成果物:
- crates/ait42-tui/src/widgets/tab_bar.rs
- crates/ait42-tui/src/widgets/sidebar.rs
- crates/ait42-tui/src/widgets/terminal_panel.rs
```

### Step 5: 統合
```bash
エージェント: FeatureBuilder
時間: 1時間

タスク:
1. EditorState拡張
   - tabs: Vec<Tab>追加
   - active_tab: usize追加
   - terminal_output: Vec<String>追加

2. Renderer統合
   - 新ウィジェット描画
   - レイアウト適用

3. イベントハンドリング
   - Ctrl+T（新タブ）
   - Ctrl+W（タブ閉じる）
   - Ctrl+`（ターミナルトグル）

成果物:
- crates/ait42-tui/src/tui_app.rs (更新)
- crates/ait42-tui/src/renderer.rs (更新)
```

### Step 6: テスト
```bash
エージェント: TestGenerator
時間: 30分

タスク:
1. レイアウトテスト
2. ウィジェット描画テスト
3. 統合テスト

成果物:
- tests/ui/cursor_layout_tests.rs
```

### Step 7: レビュー
```bash
エージェント: CodeReviewer
時間: 20分

タスク:
1. コード品質チェック
2. パフォーマンスレビュー
3. 改善提案

成果物:
- docs/reviews/phase10_review.md
```

## ファイル構成

```
crates/ait42-tui/
├── src/
│   ├── layout.rs                    # 更新: タブ・ターミナル対応
│   ├── tui_app.rs                   # 更新: 状態管理拡張
│   ├── renderer.rs                  # 更新: 新ウィジェット描画
│   ├── themes/
│   │   ├── mod.rs
│   │   ├── default.rs
│   │   └── cursor.rs                # 新規: Cursorテーマ
│   └── widgets/
│       ├── mod.rs                   # 更新: 新ウィジェット追加
│       ├── tab_bar.rs               # 新規: タブバー
│       ├── sidebar.rs               # 新規: サイドバー
│       └── terminal_panel.rs        # 新規: ターミナル
├── tests/
│   └── ui/
│       ├── cursor_layout_tests.rs   # 新規
│       └── widget_tests.rs          # 新規
└── docs/
    └── architecture/
        └── cursor_ui_design.md      # 新規
```

## データ構造

### Tab構造体
```rust
pub struct Tab {
    pub id: usize,
    pub title: String,
    pub file_path: Option<PathBuf>,
    pub modified: bool,
    pub buffer: Buffer,
}
```

### EditorState拡張
```rust
pub struct EditorState {
    // 既存フィールド
    editor: Editor,
    buffer: Buffer,
    cursor: Cursor,
    view: ViewState,
    mode: Mode,
    running: bool,

    // 新規フィールド
    tabs: Vec<Tab>,
    active_tab: usize,
    terminal_visible: bool,
    terminal_output: Vec<String>,
    sidebar_visible: bool,
    file_tree: FileTree,
}
```

### LayoutConfig拡張
```rust
pub struct LayoutConfig {
    // 既存
    show_line_numbers: bool,
    line_number_width: u16,
    show_command_palette: bool,
    command_palette_height: u16,
    show_sidebar: bool,
    sidebar_width: u16,

    // 新規
    show_tabs: bool,
    tab_bar_height: u16,
    show_terminal: bool,
    terminal_height: u16,
}
```

## カラースキーム（Cursor風）

```rust
// Cursor Dark Theme
pub const BACKGROUND: Color = Color::Rgb(30, 30, 30);        // #1E1E1E
pub const FOREGROUND: Color = Color::Rgb(204, 204, 204);     // #CCCCCC
pub const SELECTION_BG: Color = Color::Rgb(38, 79, 120);     // #264F78
pub const LINE_NUMBER: Color = Color::Rgb(133, 133, 133);    // #858585
pub const ACTIVE_LINE: Color = Color::Rgb(40, 40, 40);       // #282828

// UI Elements
pub const TAB_ACTIVE_BG: Color = Color::Rgb(45, 45, 45);     // #2D2D2D
pub const TAB_INACTIVE_BG: Color = Color::Rgb(37, 37, 37);   // #252525
pub const SIDEBAR_BG: Color = Color::Rgb(37, 37, 37);        // #252525
pub const TERMINAL_BG: Color = Color::Rgb(30, 30, 30);       // #1E1E1E
pub const BORDER: Color = Color::Rgb(60, 60, 60);            // #3C3C3C

// Syntax
pub const KEYWORD: Color = Color::Rgb(197, 134, 192);        // #C586C0
pub const STRING: Color = Color::Rgb(206, 145, 120);         // #CE9178
pub const COMMENT: Color = Color::Rgb(106, 153, 85);         // #6A9955
pub const FUNCTION: Color = Color::Rgb(220, 220, 170);       // #DCDCAA
```

## キーバインディング（Phase 10b）

```
Ctrl+T          新しいタブ
Ctrl+W          タブを閉じる
Ctrl+Tab        次のタブ
Ctrl+Shift+Tab  前のタブ
Ctrl+`          ターミナルトグル
Ctrl+B          サイドバートグル
Ctrl+E          ファイルツリーフォーカス
F2              ファイル名変更
```

## 実装の優先順位

### Priority 1: 必須（Phase 10a）
1. ✅ レイアウト拡張
2. ✅ TabBarウィジェット
3. ✅ Sidebarウィジェット
4. ✅ TerminalPanelウィジェット
5. ✅ Cursorテーマ
6. ✅ 統合・描画

### Priority 2: 推奨（Phase 10b）
7. ⏳ タブ切り替え機能
8. ⏳ ファイルツリーナビゲーション
9. ⏳ ターミナルコマンド実行
10. ⏳ キーボードショートカット

### Priority 3: オプション（Phase 10c）
11. ⏳ パネルリサイズ
12. ⏳ ドラッグ&ドロップ
13. ⏳ マウスサポート
14. ⏳ コンテキストメニュー

## エージェント実行コマンド

### 並列実行（推奨）
```bash
# Tmuxで6つのエージェントを並列実行
./scripts/tmux-parallel-agents.sh \
  system-architect "Cursor風UIアーキテクチャ設計" \
  ui-ux-designer "Cursorテーマとカラースキーム設計" \
  frontend-developer "タブバー・サイドバー・ターミナルウィジェット実装" \
  feature-builder "レイアウト拡張とマルチパネル統合" \
  test-generator "UIテストコード生成" \
  code-reviewer "実装コードレビュー"
```

### 順次実行
```bash
# Step 1: 設計
./scripts/tmux-single-agent.sh system-architect \
  "Cursor風3ペインレイアウトのアーキテクチャ設計を行い、docs/architecture/cursor_ui_design.mdを作成"

# Step 2: テーマ
./scripts/tmux-single-agent.sh ui-ux-designer \
  "Cursor風ダークテーマを設計し、crates/ait42-tui/src/themes/cursor.rsを実装"

# Step 3: レイアウト
./scripts/tmux-single-agent.sh feature-builder \
  "layout.rsを拡張してタブバー・ターミナルパネルに対応"

# Step 4: ウィジェット
./scripts/tmux-single-agent.sh frontend-developer \
  "TabBar、Sidebar、TerminalPanelウィジェットを実装"

# Step 5: 統合
./scripts/tmux-single-agent.sh feature-builder \
  "新ウィジェットをtui_app.rsとrenderer.rsに統合"

# Step 6: テスト
./scripts/tmux-single-agent.sh test-generator \
  "UIテストコードを生成"

# Step 7: レビュー
./scripts/tmux-single-agent.sh code-reviewer \
  "Phase 10実装コードをレビュー"
```

## 期待される成果

### UI表示
```
起動時:
┌─────────────────────────────────────────────────────────┐
│ 🅰 AIT42              main.rs                       × □ ─│
├─────────┬───────────────────────────────────────────────┤
│ 📁 FILES│  src/main.rs                          1:1 100%│
│ ─────── │                                               │
│ ▼ src   │  1  fn main() {                               │
│   main.rs  2    println!("Hello, World!");              │
│   lib.rs│  3  }                                         │
│         │                                               │
├─────────┼───────────────────────────────────────────────┤
│ TERMINAL│ $                                             │
└─────────┴───────────────────────────────────────────────┘
```

### パフォーマンス目標
- 起動時間: < 100ms
- レンダリング: 60 FPS
- メモリ使用量: < 50MB

## 注意事項

### Phase 10aの制限
- **UI表示のみ**（インタラクションなし）
- タブは1つ固定（切り替え不可）
- ファイルツリーは静的表示
- ターミナルは出力表示のみ

### Phase 10bで実装
- タブ切り替え機能
- ファイル選択・開く
- ターミナルコマンド実行
- キーバインディング

## 次のステップ

1. この計画書をコミット
2. 次回セッションでエージェント実行
3. 各エージェントの成果物を統合
4. ビルド & テスト
5. AIT42アプリでCursor風UIを確認！

---

**作成日**: 2025-11-03
**作成者**: Claude Code + Task Master AI
**ステータス**: Planning Phase
**次回実装**: Phase 10a実装開始
