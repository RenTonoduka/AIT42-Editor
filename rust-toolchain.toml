# Rust toolchain configuration
# Pins the Rust version for consistent builds

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy", "rust-analyzer"]
targets = ["x86_64-apple-darwin", "aarch64-apple-darwin"]
profile = "minimal"
